# linux-static-ip-subnet-lab
Built a Linux-based networking lab to simulate subnet segmentation using CIDR notation. Configured multiple static IP addresses within a /28 subnet inside a /24 network, verified network and broadcast boundaries, and used Nmap to confirm scoped host discovery. Demonstrated practical understanding of subnetting, IP addressing, &amp; routing.
